# 知识库功能测试指南

## 测试步骤

### 1. 准备测试文档

创建一个简单的测试文件 `test.txt`：

```
DeFi 是去中心化金融的缩写。

DeFi 的主要特点：
1. 无需中介机构
2. 透明度高
3. 全球可访问
4. 可编程性强

常见的 DeFi 应用包括：
- 去中心化交易所 (DEX)
- 借贷协议
- 流动性挖矿
- 稳定币
```

### 2. 上传文档

1. 打开前端 http://localhost:3000/ai
2. 连接钱包
3. 点击左侧"知识库"按钮
4. 点击"上传文档"按钮
5. 选择 `test.txt` 文件上传

**前端控制台日志**（按 F12 查看）：
```
[知识库] 开始上传文件: test.txt 钱包地址: xxxxx
[知识库] 上传成功: {...}
[知识库] 加载文档列表，钱包地址: xxxxx
[知识库] 文档列表: [...]
```

**后端控制台日志**：
```
[知识库上传] 开始处理，文件: test.txt, 钱包: xxxxx
[知识库上传] 文件大小: 256 字节
[知识库上传] 保存文件到: data/uploads/xxxxx/test.txt
[知识库上传] 文档记录已创建，ID: 1
[知识库上传] 开始解析文档...
[知识库上传] 文档解析完成，文本长度: 256
[知识库上传] 文本分块完成，共 1 个块
[知识库上传] 文本块已保存到数据库
[知识库上传] 原始文件已删除
```

### 3. 测试知识库检索

在对话框中输入：**"什么是 DeFi？"**

**前端控制台日志**：
```
[AI对话] 发送消息: {
  message: "什么是 DeFi？",
  wallet_address: "xxxxx",
  session_id: "..."
}
```

**后端控制台日志**：
```
[ExplanationAgent] 准备检索 RAG，用户输入: 什么是 DeFi？, 钱包: xxxxx
[RAG] 开始检索知识，查询: 什么是 DeFi？, 钱包: xxxxx
[UserKnowledge] 开始检索，用户: xxxxx, 查询: 什么是 DeFi？
[UserKnowledge] 调用 API: http://localhost:8000/api/v1/knowledge/search
[知识库搜索] 开始搜索，钱包: xxxxx, 查询: 什么是 DeFi？, top_k: 3
[知识库搜索] 找到 1 个文档
[知识库搜索] 找到 1 个文本块
[知识库搜索] 查询关键词: {'什么', '是', 'defi', '？'}
[知识库搜索] 匹配到 1 个相关文本块
[知识库搜索] 返回 1 个结果
[知识库搜索] 第一个结果预览: DeFi 是去中心化金融的缩写...
[UserKnowledge] API 响应状态: 200
[UserKnowledge] 找到 1 个匹配结果
[UserKnowledge] 结果 1: test.txt, 评分: 1, 内容长度: 256
[UserKnowledge] 返回结果总长度: 300
[UserKnowledge] 结果预览: 以下是你上传的文档中的相关内容：【test.txt】DeFi 是去中心化金融的缩写...
[RAG] 用户知识库检索结果长度: 300
[RAG] 检索到的内容预览: 以下是你上传的文档中的相关内容：【test.txt】DeFi 是去中心化金融的缩写...
[ExplanationAgent] RAG 上下文已添加，长度: 300
```

### 4. 验证 AI 回复

AI 的回复应该包含你上传的文档内容，例如：

```
DeFi 就是去中心化金融，简单来说就是不需要银行、券商这些中介机构，
直接在区块链上进行金融活动。

主要特点包括：
• 不需要中介，点对点交易
• 所有交易公开透明
• 全球任何人都能用
• 可以编程实现自动化

常见的应用有去中心化交易所、借贷平台、流动性挖矿等。
```

## 常见问题排查

### 问题 1：上传后找不到文档

**检查**：
1. 前端控制台是否显示 `[知识库] 上传成功`
2. 后端是否显示 `[知识库上传] 文本块已保存到数据库`
3. 数据库中是否有记录：
   ```sql
   SELECT * FROM knowledge_documents WHERE user_id = 'your_wallet_address';
   SELECT * FROM knowledge_chunks WHERE document_id = 1;
   ```

### 问题 2：AI 回复中没有使用知识库内容

**检查**：
1. 后端日志是否显示 `[RAG] 用户知识库检索结果长度: 0`
2. 如果是 0，检查：
   - 钱包地址是否正确传递
   - 查询关键词是否匹配文档内容
   - 数据库中是否有对应的文本块

**调试命令**：
```bash
# 直接测试搜索接口
curl "http://localhost:8000/api/v1/knowledge/search?wallet_address=YOUR_WALLET&query=DeFi&top_k=3"
```

### 问题 3：关键词匹配不准确

当前使用简单的关键词匹配，改进方案：
1. 使用 FAISS 向量检索（需要向量化）
2. 使用全文搜索（PostgreSQL FTS）
3. 使用更智能的分词（jieba 中文分词）

## 日志级别说明

- `INFO` - 正常流程日志
- `WARNING` - 警告（如 API 调用失败但有降级方案）
- `ERROR` - 错误（需要修复的问题）

## 性能优化建议

1. **缓存搜索结果**：相同查询可以缓存 5 分钟
2. **异步处理**：文档解析和分块可以放到后台任务
3. **向量检索**：使用 FAISS 或 Milvus 提升检索准确度
4. **分页加载**：文档列表支持分页

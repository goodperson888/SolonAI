# Solon AI 团队协作与项目管理规范

## 一、项目整体架构设计

### 1.1 技术架构分层（Monorepo架构）

```
solon-ai/
├── apps/                          # 应用层
│   ├── web/                       # Next.js前端应用
│   └── api/                       # FastAPI后端应用
├── packages/                      # 共享包
│   ├── ui/                        # 共享UI组件库
│   ├── types/                     # TypeScript类型定义
│   ├── utils/                     # 工具函数
│   └── config/                    # 共享配置
├── services/                      # 后端服务层
│   ├── ai-agents/                 # AI智能体服务（LangGraph核心）
│   ├── blockchain/                # 区块链交互服务
│   ├── risk-control/              # 风控服务
│   └── data-aggregator/           # 数据聚合服务
├── docs/                          # 项目文档
├── scripts/                       # 脚本工具
└── .github/                       # GitHub配置
    └── workflows/                 # CI/CD配置
```

### 1.2 技术栈确认

**前端（3-4人）**
- Next.js 14 + TypeScript + TailwindCSS
- Solana Web3.js + Wallet Adapter
- Zustand（状态管理）
- React Query（数据请求）

**后端API（2-3人）**
- FastAPI + Python 3.11+
- JWT认证 + Upstash Redis（免费）
- Supabase PostgreSQL（免费1GB）+ Pinecone免费版/Supabase pgvector（向量数据库）

**AI智能体（3-4人）**
- LangGraph + LangChain
- 豆包免费额度/通义千问免费版/DeepSeek API
- RAG知识库构建

**区块链交互（2-3人）**
- Solana Web3.py
- Jupiter SDK
- Alchemy免费版/Solana官方免费RPC

**DevOps（1人）**
- Docker + Docker Compose（本地开发）
- GitHub Actions（免费2000分钟/月）
- Vercel（前端）+ Railway/Render（后端）免费部署

---

## 三、Git工作流规范

### 3.1 分支管理策略（Git Flow简化版）

```
main（主分支）🔒 受保护
  ├── develop（开发分支）🔒 受保护
  │     ├── feature/模块名-功能名（功能分支）
  │     ├── fix/模块名-bug描述（修复分支）
  │     └── refactor/模块名-重构内容（重构分支）
  ├── release/v1.0.0（发布分支）
  └── hotfix/紧急修复描述（热修复分支）
```

**分支说明**

| 分支 | 用途 | 从哪里拉 | 合并到哪里 | 是否受保护 | 谁可以合并 |
|:---|:---|:---|:---|:---|:---|
| `main` | 生产环境，线上运行的代码 | - | - | 🔒 受保护 | 项目负责人 |
| `develop` | 开发主分支，最新的开发代码 | `main` | - | 🔒 受保护 | 各组Leader |
| `feature/*` | 新功能开发 | `develop` | `develop` | ❌ 不保护 | 开发者自己 |
| `fix/*` | Bug修复 | `develop` | `develop` | ❌ 不保护 | 开发者自己 |
| `refactor/*` | 代码重构 | `develop` | `develop` | ❌ 不保护 | 开发者自己 |
| `release/*` | 发布准备，测试和修复 | `develop` | `main` + `develop` | ❌ 不保护 | 项目负责人 |
| `hotfix/*` | 线上紧急修复 | `main` | `main` + `develop` | ❌ 不保护 | 项目负责人 |

**分支保护规则详解**

#### `main` 分支（最严格）
- ❌ 禁止直接 push
- ❌ 禁止 force push
- ❌ 禁止删除分支
- ✅ 只能通过 PR 合并
- ✅ PR 必须通过所有 CI 检查（lint、build）
- ✅ PR 必须至少 1 人 Review 通过
- ✅ PR 必须是最新代码（up to date）
- ✅ 只接受来自 `release/*` 或 `hotfix/*` 的 PR

#### `develop` 分支（中等严格）
- ❌ 禁止直接 push
- ❌ 禁止 force push
- ✅ 只能通过 PR 合并
- ✅ PR 必须通过所有 CI 检查（lint）
- ✅ PR 必须至少 1 人 Review 通过
- ✅ 接受来自 `feature/*`、`fix/*`、`refactor/*` 的 PR

#### 其他分支（不保护）
- 开发者自由创建和删除
- 命名必须符合规范
- 完成后及时删除

**完整的分支工作流程**

```
1. 日常开发流程:
   develop → feature/xxx → 开发 → PR → Review → 合并到 develop → 删除 feature

2. 发布流程:
   develop → release/v1.0 → 测试修复 → PR → 合并到 main + develop → 打 Tag

3. 紧急修复流程:
   main → hotfix/xxx → 修复 → PR → 合并到 main + develop → 打 Tag
```

**GitHub 分支保护设置步骤**

这些规则需要在 GitHub 仓库中手动设置（不支持命令行配置）：

**步骤1: 进入设置**
```
GitHub 仓库页面 → Settings → Branches → Add branch protection rule
```

**步骤2: 配置 main 分支保护**
```
Branch name pattern: main

勾选以下选项:
☑ Require a pull request before merging
  ☑ Require approvals: 1
  ☑ Dismiss stale pull request approvals when new commits are pushed
☑ Require status checks to pass before merging
  ☑ Require branches to be up to date before merging
  搜索并添加: frontend-lint, backend-lint, commitlint, frontend-build
☑ Require conversation resolution before merging
☑ Do not allow bypassing the above settings
```

**步骤3: 配置 develop 分支保护**
```
Branch name pattern: develop

勾选以下选项:
☑ Require a pull request before merging
  ☑ Require approvals: 1
☑ Require status checks to pass before merging
  搜索并添加: frontend-lint, backend-lint
```

**注意**: Status checks 只有在第一次 CI 运行后才会出现在列表中。所以需要先推送代码触发一次 CI，然后再回来设置。

**推荐的设置顺序**:
1. 先推送代码到 GitHub
2. 创建一个测试 PR 触发 CI
3. CI 运行完成后，再去设置分支保护规则
4. 在 Status checks 中选择需要通过的检查项

### 3.2 分支命名规范

```bash
# 功能分支
feature/wallet-connect          # 钱包连接功能
feature/asset-scan              # 资产扫描功能
feature/strategy-generation     # 策略生成功能

# 修复分支
fix/wallet-disconnect-bug       # 修复钱包断开连接bug
fix/api-timeout                 # 修复API超时问题

# 重构分支
refactor/agent-architecture     # 重构智能体架构
```

### 3.3 Commit提交规范（约定式提交）

**格式**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type类型**
- `feat`: 新功能
- `fix`: Bug修复
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 重构（既不是新功能也不是修复）
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具/依赖更新

**示例**
```bash
feat(wallet): 添加Phantom钱包连接功能

- 实现钱包连接组件
- 添加钱包状态管理
- 集成Solana Wallet Adapter

Closes #123

---

fix(api): 修复资产查询接口超时问题

将超时时间从5s调整为15s，优化查询逻辑

Fixes #456
```

### 3.4 代码提交流程

```bash
# 1. 从develop拉取最新代码
git checkout develop
git pull origin develop

# 2. 创建功能分支
git checkout -b feature/wallet-connect

# 3. 开发功能，频繁提交
git add .
git commit -m "feat(wallet): 添加钱包连接基础组件"

# 4. 开发完成，推送到远程
git push origin feature/wallet-connect

# 5. 在GitHub上创建Pull Request到develop分支

# 6. Code Review通过后，合并到develop

# 7. 删除本地和远程功能分支
git branch -d feature/wallet-connect
git push origin --delete feature/wallet-connect
```

### 3.5 Pull Request规范

**PR标题格式**
```
[模块名] 功能描述
```

**PR描述模板**
```markdown
## 功能描述
简要描述本次PR的功能或修复内容

## 变更内容
- [ ] 新增XXX功能
- [ ] 修复XXX问题
- [ ] 优化XXX性能

## 测试情况
- [ ] 本地测试通过
- [ ] 单元测试通过
- [ ] 集成测试通过

## 截图/录屏
（如果是UI相关，提供截图或录屏）

## 关联Issue
Closes #123
```

**Code Review要求**
- 每个PR必须至少1人Review通过才能合并
- Leader必须Review核心模块的PR
- Review重点：代码规范、逻辑正确性、性能问题、安全风险

---

## 四、代码规范

### 4.1 前端代码规范

**目录结构**
```
apps/web/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/            # 认证相关页面
│   │   ├── dashboard/         # 仪表盘
│   │   └── layout.tsx
│   ├── components/            # 组件
│   │   ├── ui/               # 基础UI组件
│   │   ├── wallet/           # 钱包相关组件
│   │   └── strategy/         # 策略相关组件
│   ├── hooks/                # 自定义Hooks
│   ├── lib/                  # 工具库
│   ├── services/             # API服务
│   ├── store/                # 状态管理
│   └── types/                # 类型定义
```

**命名规范**
```typescript
// 组件：PascalCase
export const WalletConnect = () => {}

// 函数/变量：camelCase
const getUserAssets = () => {}
const walletAddress = "xxx"

// 常量：UPPER_SNAKE_CASE
const API_BASE_URL = "https://api.solon.ai"

// 类型/接口：PascalCase
interface UserAsset {
  token: string
  amount: number
}

// 文件名：kebab-case
wallet-connect.tsx
use-wallet.ts
```

**组件规范**
```typescript
// 使用函数组件 + TypeScript
interface WalletConnectProps {
  onConnect?: (address: string) => void
}

export const WalletConnect: React.FC<WalletConnectProps> = ({ onConnect }) => {
  // 1. Hooks
  const { connect, connected } = useWallet()

  // 2. 状态
  const [loading, setLoading] = useState(false)

  // 3. 副作用
  useEffect(() => {
    // ...
  }, [])

  // 4. 事件处理
  const handleConnect = async () => {
    // ...
  }

  // 5. 渲染
  return (
    <button onClick={handleConnect}>
      {connected ? 'Connected' : 'Connect Wallet'}
    </button>
  )
}
```

### 4.2 后端代码规范

**目录结构**
```
apps/api/
├── app/
│   ├── api/                   # API路由
│   │   ├── v1/
│   │   │   ├── auth.py       # 认证相关
│   │   │   ├── assets.py     # 资产相关
│   │   │   └── strategy.py   # 策略相关
│   ├── core/                  # 核心配置
│   │   ├── config.py
│   │   ├── security.py
│   │   └── database.py
│   ├── models/                # 数据模型
│   ├── schemas/               # Pydantic模型
│   ├── services/              # 业务逻辑
│   └── utils/                 # 工具函数
├── tests/                     # 测试
└── main.py                    # 入口文件
```

**命名规范**
```python
# 类：PascalCase
class UserService:
    pass

# 函数/变量：snake_case
def get_user_assets():
    pass

user_address = "xxx"

# 常量：UPPER_SNAKE_CASE
API_VERSION = "v1"
MAX_RETRY_COUNT = 3

# 文件名：snake_case
user_service.py
wallet_utils.py
```

**API路由规范**
```python
from fastapi import APIRouter, Depends
from app.schemas.asset import AssetResponse
from app.services.asset_service import AssetService

router = APIRouter(prefix="/api/v1/assets", tags=["assets"])

@router.get("/{wallet_address}", response_model=AssetResponse)
async def get_assets(
    wallet_address: str,
    service: AssetService = Depends()
):
    """
    获取钱包资产

    Args:
        wallet_address: 钱包地址

    Returns:
        AssetResponse: 资产信息
    """
    return await service.get_assets(wallet_address)
```

### 4.3 AI智能体代码规范

**目录结构**
```
services/ai-agents/
├── agents/                    # 各类Agent
│   ├── intent_agent.py       # 意图理解Agent
│   ├── strategy_agent.py     # 策略生成Agent
│   └── risk_agent.py         # 风控Agent
├── graphs/                    # LangGraph流程
│   └── main_graph.py
├── prompts/                   # Prompt模板
├── tools/                     # Agent工具
├── rag/                       # RAG知识库
│   ├── loaders/              # 文档加载器
│   ├── embeddings/           # 向量化
│   └── retrievers/           # 检索器
└── utils/
```

**Agent开发规范**
```python
from langchain.agents import AgentExecutor
from langchain.prompts import ChatPromptTemplate
from typing import Dict, Any

class StrategyAgent:
    """策略生成Agent"""

    def __init__(self, llm, tools):
        self.llm = llm
        self.tools = tools
        self.prompt = self._load_prompt()

    def _load_prompt(self) -> ChatPromptTemplate:
        """加载Prompt模板"""
        return ChatPromptTemplate.from_messages([
            ("system", "你是Solana DeFi策略专家..."),
            ("user", "{input}")
        ])

    async def generate_strategy(
        self,
        user_input: Dict[str, Any]
    ) -> Dict[str, Any]:
        """
        生成投资策略

        Args:
            user_input: 用户输入（风险偏好、资金规模等）

        Returns:
            策略详情
        """
        # 1. 参数校验
        self._validate_input(user_input)

        # 2. 调用LLM生成策略
        result = await self._invoke_llm(user_input)

        # 3. 结果校验
        validated_result = self._validate_output(result)

        return validated_result

    def _validate_input(self, user_input: Dict[str, Any]):
        """输入校验"""
        required_fields = ["risk_level", "amount"]
        for field in required_fields:
            if field not in user_input:
                raise ValueError(f"Missing required field: {field}")

    def _validate_output(self, result: Dict[str, Any]) -> Dict[str, Any]:
        """输出校验（防止幻觉）"""
        # 校验逻辑
        return result
```

### 4.4 通用规范

**注释规范**
```typescript
// TypeScript/JavaScript
/**
 * 获取用户资产
 * @param walletAddress - 钱包地址
 * @returns 资产列表
 */
async function getUserAssets(walletAddress: string): Promise<Asset[]> {
  // 实现
}
```

```python
# Python
def get_user_assets(wallet_address: str) -> List[Asset]:
    """
    获取用户资产

    Args:
        wallet_address: 钱包地址

    Returns:
        List[Asset]: 资产列表

    Raises:
        ValueError: 钱包地址无效
    """
    pass
```

**错误处理规范**
```typescript
// 前端
try {
  const assets = await fetchAssets(address)
} catch (error) {
  console.error('Failed to fetch assets:', error)
  toast.error('获取资产失败，请稍后重试')
}
```

```python
# 后端
from app.core.exceptions import AssetNotFoundError

try:
    assets = await asset_service.get_assets(wallet_address)
except AssetNotFoundError as e:
    raise HTTPException(status_code=404, detail=str(e))
except Exception as e:
    logger.error(f"Unexpected error: {e}")
    raise HTTPException(status_code=500, detail="Internal server error")
```

---

## 五、开发流程与协作规范

### 5.1 每日站会（15分钟）

**时间**：每天上午10:00

**参与人员**：全体开发人员

**会议内容**（每人3分钟）
1. 昨天完成了什么
2. 今天计划做什么
3. 遇到什么阻碍

**会议记录**：项目负责人记录到飞书/钉钉文档

### 5.2 开发迭代流程（2周一个Sprint）

**Week 1**
- Day 1-2：需求评审、技术方案设计、任务拆分
- Day 3-7：功能开发、每日站会、问题同步

**Week 2**
- Day 8-11：功能开发、联调测试
- Day 12-13：集成测试、Bug修复
- Day 14：Sprint回顾、Demo演示、下个Sprint规划

### 5.3 任务管理（使用GitHub Projects或Jira）

**任务状态流转**
```
待办 → 进行中 → 开发完成 → 测试中 → 已完成
```

**任务优先级**
- P0：阻塞性问题，必须立即解决
- P1：核心功能，本Sprint必须完成
- P2：重要功能，尽量本Sprint完成
- P3：优化项，可延后到下个Sprint

**任务模板**
```markdown
## 任务标题
[前端] 实现钱包连接功能

## 任务描述
实现Phantom和Solflare钱包的连接功能

## 验收标准
- [ ] 用户可以点击按钮连接钱包
- [ ] 连接成功后显示钱包地址
- [ ] 支持断开连接
- [ ] 错误处理完善

## 预计工时
2天

## 负责人
@张三

## 依赖任务
无
```

### 5.4 代码Review流程

**Review清单**
- [ ] 代码符合规范
- [ ] 逻辑正确无bug
- [ ] 有必要的注释
- [ ] 无安全风险
- [ ] 性能无明显问题
- [ ] 测试覆盖充分

**Review时效**
- 普通PR：24小时内完成Review
- 紧急PR：4小时内完成Review

### 5.5 测试规范

**单元测试**
- 核心业务逻辑必须有单元测试
- 测试覆盖率目标：60%以上

**集成测试**
- 每个API接口必须有集成测试
- 关键业务流程必须有端到端测试

**测试命名**
```typescript
// 前端
describe('WalletConnect', () => {
  it('should connect wallet successfully', async () => {
    // 测试逻辑
  })

  it('should handle connection error', async () => {
    // 测试逻辑
  })
})
```

```python
# 后端
def test_get_assets_success():
    """测试获取资产成功场景"""
    pass

def test_get_assets_invalid_address():
    """测试无效地址场景"""
    pass
```

---

## 六、环境配置与部署

### 6.1 开发环境配置

**前端环境变量（.env.local）**
```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com
NEXT_PUBLIC_WALLET_ADAPTER_NETWORK=devnet
```

**后端环境变量（.env）**
```bash
# 数据库（Supabase免费版）
DATABASE_URL=postgresql://user:password@db.xxx.supabase.co:5432/postgres
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=your_supabase_key

# Redis（Upstash免费版）
UPSTASH_REDIS_URL=https://xxx.upstash.io
UPSTASH_REDIS_TOKEN=your_token

# Solana（Alchemy免费版 + 官方RPC备用）
ALCHEMY_RPC_URL=https://solana-mainnet.g.alchemy.com/v2/your_api_key
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_DEVNET_RPC_URL=https://api.devnet.solana.com

# AI模型（豆包免费额度 + DeepSeek备用）
DOUBAO_API_KEY=your_api_key
DOUBAO_API_URL=https://ark.cn-beijing.volces.com/api/v3
DEEPSEEK_API_KEY=your_api_key
DEEPSEEK_API_URL=https://api.deepseek.com

# 向量数据库（Pinecone免费版 或 Supabase pgvector）
PINECONE_API_KEY=your_api_key
PINECONE_ENVIRONMENT=us-west1-gcp-free
# 或使用 Supabase pgvector（无需额外配置）

# JWT
SECRET_KEY=your_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### 6.2 免费部署方案（推荐用于MVP阶段）

**方案一：完全云端免费部署（推荐）**

| 服务 | 平台 | 免费额度 | 部署方式 |
|:---|:---|:---|:---|
| 前端 | Vercel | 100GB带宽/月 | 连接GitHub自动部署 |
| 后端API | Railway.app | 500小时/月（约20天） | 连接GitHub自动部署 |
| 数据库 | Supabase | 1GB存储 | 在线创建项目 |
| Redis | Upstash | 10000次/天 | 在线创建数据库 |
| 向量数据库 | Pinecone | 1个索引，100万向量 | 在线创建索引 |

**部署步骤**
1. **前端部署到Vercel**
   ```bash
   # 1. 在Vercel官网连接GitHub仓库
   # 2. 选择apps/web目录
   # 3. 配置环境变量
   # 4. 点击Deploy，自动部署
   ```

2. **后端部署到Railway**
   ```bash
   # 1. 在Railway官网连接GitHub仓库
   # 2. 选择apps/api目录
   # 3. 配置环境变量
   # 4. 自动检测FastAPI并部署
   ```

3. **数据库配置**
   - Supabase：注册 → 创建项目 → 复制DATABASE_URL
   - Upstash Redis：注册 → 创建数据库 → 复制连接信息
   - Pinecone：注册 → 创建索引 → 复制API Key

**方案二：本地开发用Docker（docker-compose.yml）**
```yaml
version: '3.8'

services:
  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://api:8000
    depends_on:
      - api

  api:
    build: ./apps/api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/solon_ai
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=solon_ai
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
```

**注意**：本地开发用Docker，生产环境用免费云服务，可以节省服务器成本。

### 6.3 CI/CD配置（GitHub Actions）

**.github/workflows/ci.yml**
```yaml
name: CI

on:
  pull_request:
    branches: [develop, main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
```

---

## 七、沟通协作工具

### 7.1 工具选型

| 工具 | 用途 | 负责人 |
|:---|:---|:---|
| 飞书/钉钉 | 日常沟通、文档协作 | 全员 |
| GitHub | 代码托管、Issue管理、PR Review | 全员 |
| Figma | UI设计、原型图 | 设计师+前端 |
| Postman | API文档、接口测试 | 后端+前端 |
| Notion/语雀 | 知识库、技术文档 | 全员 |

### 7.2 沟通规范

**同步沟通**
- 紧急问题：直接语音/电话
- 一般问题：群聊讨论
- 技术方案：会议讨论

**异步沟通**
- 非紧急问题：GitHub Issue
- 需求变更：文档记录+通知
- 技术分享：文档沉淀

**响应时效**
- P0问题：30分钟内响应
- P1问题：2小时内响应
- 一般问题：当天响应

---

## 八、风险管理

### 8.1 技术风险

| 风险 | 应对措施 |
|:---|:---|
| API限流 | 多账号轮询、本地缓存 |
| RPC节点不稳定 | 配置多个备用节点 |
| 大模型幻觉 | RAG+结果校验+人工审核 |
| 性能瓶颈 | 提前压测、优化热点代码 |

### 8.2 进度风险

| 风险 | 应对措施 |
|:---|:---|
| 功能做不完 | 按优先级砍功能，保证核心闭环 |
| 人员请假 | 交叉培训、文档完善 |
| 技术难点卡住 | 及时求助、调整方案 |

### 8.3 质量风险

| 风险 | 应对措施 |
|:---|:---|
| Bug太多 | 加强Code Review、增加测试 |
| 性能差 | 提前性能测试、优化关键路径 |
| 安全漏洞 | 安全扫描、代码审计 |

---

## 九、免费资源清单与成本规划

### 9.1 完整免费技术栈

**前端部署**
- **Vercel**（推荐）
  - 免费额度：100GB带宽/月，无限请求
  - 自动CI/CD，连接GitHub自动部署
  - 自带CDN加速
  - 注册地址：https://vercel.com

**后端部署**
- **Railway.app**（推荐）
  - 免费额度：500小时/月（约20天在线）
  - 支持FastAPI自动识别
  - 自动HTTPS证书
  - 注册地址：https://railway.app
- **Render.com**（备选）
  - 免费额度：750小时/月
  - 15分钟无请求会休眠
  - 注册地址：https://render.com

**数据库**
- **Supabase PostgreSQL**（推荐）
  - 免费额度：1GB数据库，2GB文件存储
  - 内置pgvector支持（可做向量数据库）
  - 自带API和实时订阅
  - 注册地址：https://supabase.com
- **PlanetScale**（备选）
  - 免费额度：5GB存储
  - MySQL兼容
  - 注册地址：https://planetscale.com

**缓存数据库**
- **Upstash Redis**
  - 免费额度：10000次请求/天
  - 全球边缘网络
  - 注册地址：https://upstash.com

**向量数据库**
- **Pinecone**（方案1）
  - 免费额度：1个索引，100万向量
  - 专业向量搜索
  - 注册地址：https://www.pinecone.io
- **Supabase + pgvector**（方案2，推荐）
  - 完全免费，无限制
  - 直接用PostgreSQL存储向量
  - 无需额外注册

**Solana RPC节点**
- **Alchemy**（推荐）
  - 免费额度：3亿次请求/月
  - 稳定性好，速度快
  - 注册地址：https://www.alchemy.com
- **Solana官方RPC**（备用）
  - 完全免费，有限流
  - Mainnet: https://api.mainnet-beta.solana.com
  - Devnet: https://api.devnet.solana.com
- **QuickNode**（备选）
  - 免费试用额度
  - 注册地址：https://www.quicknode.com

**大模型API**
- **豆包Doubao**（推荐）
  - 新用户大量免费额度
  - 国内访问快
  - 注册地址：https://www.volcengine.com/product/doubao
- **通义千问**（备选）
  - 免费版：100万tokens/天
  - 注册地址：https://dashscope.aliyun.com
- **DeepSeek**（备选）
  - 价格极低，几乎免费
  - 注册地址：https://platform.deepseek.com

**监控告警**
- **Sentry**
  - 免费额度：5000错误/月
  - 错误追踪和性能监控
  - 注册地址：https://sentry.io
- **Better Uptime**
  - 免费额度：10个监控
  - 网站可用性监控
  - 注册地址：https://betteruptime.com

**CI/CD**
- **GitHub Actions**
  - 免费额度：2000分钟/月
  - 自动化测试和部署
  - GitHub自带

**域名（可选）**
- **Freenom**：免费域名（.tk/.ml/.ga等）
- **Namecheap**：便宜域名（$1-2/年）
- 或直接用Vercel/Railway提供的免费子域名

### 9.2 免费方案的限制与应对

| 资源 | 免费限制 | 是否够用MVP | 超限后方案 |
|:---|:---|:---|:---|
| Vercel带宽 | 100GB/月 | ✅ 够用（约10万次页面访问） | 升级到Pro版$20/月 |
| Railway运行时 | 500小时/月 | ⚠️ 约20天（需优化） | 升级到$5/月（无限时长） |
| Supabase数据库 | 1GB存储 | ✅ 够用（约10万条记录） | 升级到Pro版$25/月 |
| Upstash Redis | 10000次/天 | ⚠️ 可能不够 | 优化缓存策略或升级到$10/月 |
| Alchemy RPC | 3亿次/月 | ✅ 完全够用 | 升级到付费版 |
| 豆包API | 新用户免费额度 | ✅ 够用前期测试 | 按量付费（成本很低） |
| Pinecone | 100万向量 | ✅ 够用 | 升级到$70/月 |

**优化建议**
1. **Railway运行时不够**：设置自动休眠，无请求时自动停止（可延长到30天+）
2. **Upstash Redis限制**：减少缓存写入，增加缓存过期时间，只缓存热点数据
3. **数据库存储不够**：定期清理历史数据，只保留近30天数据

### 9.3 成本规划（分阶段）

**阶段1：MVP测试（0-100用户，0-2周）**
- 全部使用免费资源
- **月成本：$0**
- 足够验证产品核心功能

**阶段2：小规模上线（100-1000用户，2-8周）**
- Railway升级到$5/月（无限运行时）
- 其他继续免费
- **月成本：$5-10**
- 可支撑1000+活跃用户

**阶段3：规模化运营（1000-10000用户，2-6个月）**
- Vercel升级到Pro版$20/月
- Railway升级到$20/月
- Supabase升级到Pro版$25/月
- 大模型API按量付费约$20/月
- **月成本：$85-100**
- 可支撑10000+活跃用户

**阶段4：大规模商业化（10000+用户）**
- 考虑自建服务器或云服务器
- 预计月成本：$300-500
- 此时应该已有稳定营收

### 9.4 免费资源注册清单（开发前必做）

**第1天：注册所有免费账号**
- [ ] GitHub账号（代码托管）
- [ ] Vercel账号（前端部署）
- [ ] Railway账号（后端部署）
- [ ] Supabase账号（数据库）
- [ ] Upstash账号（Redis）
- [ ] Alchemy账号（Solana RPC）
- [ ] 豆包/通义千问账号（大模型）
- [ ] Sentry账号（错误监控）

**第2天：配置所有服务**
- [ ] Supabase创建项目，获取DATABASE_URL
- [ ] Upstash创建Redis数据库，获取连接信息
- [ ] Alchemy创建Solana应用，获取RPC URL
- [ ] 豆包/通义千问获取API Key
- [ ] 配置Pinecone索引（或使用Supabase pgvector）

**第3天：连接GitHub并测试部署**
- [ ] Vercel连接GitHub仓库，测试前端部署
- [ ] Railway连接GitHub仓库，测试后端部署
- [ ] 配置所有环境变量
- [ ] 测试完整部署流程

### 9.5 免费额度监控（避免超限）

**设置告警**
- Vercel：在Dashboard查看带宽使用情况
- Railway：在Dashboard查看运行时使用情况
- Supabase：在Dashboard查看数据库大小
- Upstash：在Dashboard查看请求次数
- Alchemy：在Dashboard查看API调用次数

**每周检查**（DevOps负责）
- 周一上午检查所有平台的使用情况
- 接近限额时提前优化或升级
- 记录到项目文档

---

## 十、附录

### 10.1 常用命令

**前端**
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 代码检查
npm run lint

# 运行测试
npm run test

# 构建生产版本
npm run build
```

**后端**
```bash
# 安装依赖
pip install -r requirements.txt

# 启动开发服务器
uvicorn app.main:app --reload

# 运行测试
pytest

# 代码格式化
black .

# 类型检查
mypy .
```

### 10.2 快速链接

- 需求文档：[需求.md](./需求.md)
- API文档：http://localhost:8000/docs（本地）或 https://your-api.railway.app/docs（生产）
- 前端预览：https://your-project.vercel.app
- Supabase Dashboard：https://app.supabase.com
- Railway Dashboard：https://railway.app/dashboard
- 设计稿：[Figma链接]
- 项目看板：[GitHub Projects链接]

### 10.3 联系方式

| 角色 | 姓名 | 联系方式 |
|:---|:---|:---|
| 项目负责人 | XXX | 微信/电话 |
| 前端Leader | XXX | 微信/电话 |
| 后端Leader | XXX | 微信/电话 |
| AI Leader | XXX | 微信/电话 |

---

**文档版本**：v2.0（免费资源版）
**最后更新**：2026-04-15
**维护人**：项目负责人

---

## 附：免费部署快速上手指南

### 1分钟快速部署前端到Vercel

```bash
# 1. 推送代码到GitHub
git push origin main

# 2. 访问 https://vercel.com
# 3. 点击 "Import Project"
# 4. 选择你的GitHub仓库
# 5. 配置：
#    - Framework Preset: Next.js
#    - Root Directory: apps/web
#    - Environment Variables: 添加NEXT_PUBLIC_API_URL等
# 6. 点击 Deploy

# 完成！获得免费域名：https://your-project.vercel.app
```

### 1分钟快速部署后端到Railway

```bash
# 1. 访问 https://railway.app
# 2. 点击 "New Project" → "Deploy from GitHub repo"
# 3. 选择你的仓库
# 4. 配置：
#    - Root Directory: apps/api
#    - Start Command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
#    - Environment Variables: 添加DATABASE_URL、REDIS_URL等
# 5. 自动部署

# 完成！获得免费域名：https://your-api.railway.app
```

### 常见问题

**Q: Railway免费500小时不够用怎么办？**
A: 在Railway设置中开启"Sleep on idle"，无请求时自动休眠，可延长到30天+

**Q: Upstash Redis每天10000次不够用怎么办？**
A: 优化缓存策略，只缓存热点数据，增加过期时间，或升级到$10/月

**Q: 如何监控免费额度使用情况？**
A: 每个平台的Dashboard都有使用情况统计，建议每周一检查一次

**Q: 什么时候需要升级付费版？**
A: 当用户量超过1000，或免费额度经常超限时，建议升级。此时产品应该已有一定营收。

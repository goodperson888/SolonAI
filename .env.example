# Docker Compose 默认环境变量
# 复制为 `.env` 后可按需修改；本地只启动 db/redis 时也会消除 Compose 变量警告。

DATABASE_URL=postgresql://solon:solon_password@db:5432/solon_ai
REDIS_URL=redis://redis:6379

SOLANA_RPC_URL=https://api.devnet.solana.com
DOUBAO_API_KEY=
SECRET_KEY=your-secret-key-change-in-production
